import React from "react";
import { View, Text, StyleSheet, TpuchnableOpacity,ScrollView } from "react-native";


const Menu = () => {

    return (
        <ScrollView>
            <Text
                style={{
                    fontSize: 30,
                    textAlign: "center",
                    marginTop: "20%",
                }}
                >PROPERTIES</Text>
        </ScrollView>
    );
    }

export default Menu;